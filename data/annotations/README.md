# Annotation Directory

Store manually checked benchmark labels here. Keep labels small and task-specific at first:

- `layout/`: page regions and view boxes
- `views/`: view count, view type, main-view labels
- `dimensions/`: OCR ground truth and normalized dimension tokens
- `features/`: feature counts and feature labels
- `ir/`: DrawingIR and ConstraintGraph ground truth

